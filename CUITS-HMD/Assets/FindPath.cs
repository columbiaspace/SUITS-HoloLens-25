using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FindPath : MonoBehaviour
{
    // Start is called before the first frame update
    public Vector2Int start;
    public int poi;

    private list pois = [(poi1), (poi2), (poi3)]; //shira will figure this out
    
    

    void Start()
    {
        request(eva pos)
        start = modify the eva pos
        request(compute path(start and end))

        print out path with gpt code 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
